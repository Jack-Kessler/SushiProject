@model IEnumerable<FoodBevIngredient>

<h2>Ingredients</h2>
<hr />
<input type="button" value="Return to Main Screen" onclick="@("window.location.href='" + @Url.Action("Index", "Home") + "'");" />
<br />
<br />
@if (User.IsInRole("Owner"))
{
    <div style="display: block">

        @using (Html.BeginForm("InsertFoodBevIngredient", "FoodBevIngredient", "Post"))
        {
            <input type="hidden" />
            <input type="submit" class="StandardButtonOutline" value="Add New Ingredient" />
        }
    </div>
    <br />
}
@if (User.IsInRole("Chef") || User.IsInRole("Manager") || User.IsInRole("Owner"))
{
    <table class="table">
        <tr>
            <th>ID</th>
            <th>Ingredient Name</th>
            <th>Ingredient Stock Level</th>
            <th>Ingredient Cost</th>
            <th>Ingredient Category</th>

        </tr>

        @foreach (var ingr in @Model)
        {
            <tr>
                <td><a href=/FoodBevIngredient/ViewFoodBevIngredient/@ingr.IngredientID>@ingr.IngredientID</a></td>
                <td>@ingr.IngredientName</td>
                <td>@ingr.IngredientStockLevel</td>
                <td>@ingr.IngredientCost</td>
                <td>@ingr.IngredientCategoryName</td>
            </tr>
        }
    </table>
}
<br />
<input type="button" class="StandardButtonOutline" value="Return to Main Screen" onclick="@("window.location.href='" + @Url.Action("Index", "Home") + "'");" />